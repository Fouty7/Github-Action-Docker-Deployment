version: '3.5'

services:
    nodejs-app:
        build:
          context: ./node-app
        container_name: nodejs-app
        hostname: nodejs-app
        ports:
            - "3000:3000"  
    nginx:
        build:
          context: ./nginx
        container_name: nginx
        hostname: nginx
        ports:
            - "80:80" 
        depends_on:
            - nodejs-app